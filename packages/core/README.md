# core

Shared reader and writer contracts for `php-data-streams`.

## What it provides

- `Reader` for lazy iteration
- `Writer` for record-by-record output
- `ConsoleWriter` for diagnostics and examples
